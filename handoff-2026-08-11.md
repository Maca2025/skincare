# HANDOFF — Skincare Tracker · 11 de agosto de 2026

> Copia local. El original vive en el proyecto de Claude como
> `claude/handoff-2026-08-11.md`, junto con `claude/cadencia-especificacion.md`
> (el diseño completo) y `claude/pendientes.md` (el backlog).

## Lo primero de mañana

`app.js` y `sw.js` de esta carpeta están MÁS ADELANTADOS que GitHub: traen la
Regla A del PDF de Stock y la caché en v26. Producción sigue en v25.

→ Subirlos con "Add file" → "Upload files", los dos en el mismo commit.

**No se puede hacer push desde el contenedor de Cowork.** Hay un proxy de git
que solo permite escribir en repos autorizados para la sesión. Un token
fine-grained NO lo resuelve — se probó el 11 de agosto y dio 403. El token que
se creó ese día fue revocado. No volver a pedir uno.

## Lo que se construyó

Botón "📄 Generar PDF del stock" en la pestaña Stock (`app.js`, bloque
`// ── PDF DEL STOCK`). Agrupa por tipo; los SPF primero ordenados por `tier`
(su potencia es null a propósito), el resto por el perfil completo de potencias
comparado lexicográficamente, con el tier solo desempatando. Cada producto una
sola vez con todos sus puntajes. Imprime el catálogo completo ignorando los
filtros de pantalla.

14 aserciones nuevas + las 3 suites de nodo (91 casos) en verde.

## Lo que se diseñó y falta construir

El motor de cadencia: reemplazar las rutinas por día de la semana por pisos y
techos semanales por eje, con el estado de piel como puerta. Todo el detalle
—incluido que `DOSE_AXES.diasIdeales` hay que partirlo en `diasPiso` y
`diasTecho`, y los 23 números ya decididos— está en
`claude/cadencia-especificacion.md` dentro del proyecto de Claude.
